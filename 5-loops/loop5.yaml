---
- name: Bucle con indice
  hosts: worker1

  tasks:
  - name: Indices
    ansible.builtin.debug:
      msg: "{{item}} esta en la posicion {{indice}}"
    loop:
        - pikacho
        - cagacho
        - mierdacho
    loop_control:
        index_var: indice
    when: indice == 1
